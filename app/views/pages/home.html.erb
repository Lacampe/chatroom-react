<%= content_tag :div,
                class: 'hook',
                chatrooms: @chatrooms.to_json,
                default_active_chatroom: @default_active_chatroom.to_json,
                current_user: current_user.to_json do %><% end %>

<%= javascript_pack_tag 'index' %>
